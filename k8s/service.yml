apiVersion: v1
kind: Service
metadata:
  name: todo-svc
  namespace: todo
spec:
  selector:
    app.kubernetes.io/name: todo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      type: NodePort